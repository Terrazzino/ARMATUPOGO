/**
 * Tipos derivados del schema de la base de datos PostgreSQL en Supabase.
 * Estos tipos se generarán automáticamente cuando se cree el schema.
 *
 * @see docs/spec.md § 4. Entidades
 * @see docs/DATABASE.md (se creará con migraciones)
 */

// Placeholder: tipos específicos de la DB se generarán posteriormente
// desde el schema de Supabase mediante herramientas como supabase-gen

// Will be populated in Phase 2: Database Schema
// export interface UserRow { ... }
// export interface ProjectRow { ... }
// export interface EventRow { ... }
